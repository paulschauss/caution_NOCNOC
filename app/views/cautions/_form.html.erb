<%= form_with(model: @caution) do |form| %>
  <% if @caution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caution.errors.count, "error") %> prohibited this caution from being saved:</h2>

      <ul>
        <% caution.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
